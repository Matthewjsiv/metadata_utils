---
data_folder: '/home/physics_atv/rosbags'
experiment_name: 'debug_planner'
pre:
  driver: Matthew
  robot: yamaha_viking
  launch:
    super_odometry:
      args:

    lidar_mapping:
      args:

              #    pointcloud_colorization:
              #      args:

    dino_mapping:
      args: 

    dino_irl:
      args:

    planner_valuemap:
      args:

    rviz:
      args:

  other:
      live_notes: 
      record:

  context:
  #keywords - collect, test, learning_physics, sara
    - 'collect'
    - 'learning_physics'
  conditions:
  #keywords - sunny, snow, muddy
    - 'sunny'
  course:
    - 'green'
