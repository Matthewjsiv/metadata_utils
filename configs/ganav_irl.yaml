---
data_folder: '/home/physics_atv/rosbags'
experiment_name: 'ganav_irl'
pre:
  driver: Matthew
  robot: yamaha_viking
  launch:
    super_odometry:
      args:

    lidar_mapping_dino:
      args:

#    pointcloud_colorization:
#      args:

    dino_mapping:
      args: 
        visual_mapping_config: ganav.yaml

    dino_irl:
      args:
        model_fp: ../models/yamaha/visual_ablations/2024-03-22-20-12-24_visual_ablations_semantics/itr_3.pt

    rviz:
      args:

  other:
      live_notes: 
#      record:

  context:
  #keywords - collect, test, learning_physics, sara
    - 'collect'
    - 'learning_physics'
  conditions:
  #keywords - sunny, snow, muddy
    - 'sunny'
  course:
    - 'green'
